import UIKit

// Define a structure to represent BMI data
struct BMI {
    // Properties to store BMI value, advice, and color
    let value: Float // BMI value
    let advice: String // Advice associated with the BMI value
    let color: UIColor // Color associated with the BMI value
}

